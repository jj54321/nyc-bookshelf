<div class="container">
	<h1>Bookshelf</h1>

	<form class="form-search">
    <div class="input-append">
        <input ng-model='query' class="span2 search-query">
        <button type="submit" class="btn">Search</button>
    </div>
</form>
<div class="btn-group" data-toggle="buttons">
  <label class="btn btn-primary active">
    <input type="checkbox" checked autocomplete="off" ng-click="genre = {genre:'action'}"> Action
  </label>
  <label class="btn btn-primary">
    <input type="checkbox" autocomplete="off" ng-click="genre = {genre:'fantasy'}"> Fantasy
  </label>
	<label class="btn btn-primary">
		<input type="checkbox" autocomplete="off" ng-click="getAll()"> All
	</label>

</div>
	<ul>
		<li ng-repeat="book in books | filter:query | filter:genre"><a ui-sref="show({id: book.id})">{{book.title}} Votes: {{book.votes.length}}</a></li>
	</ul>
			<a ui-sref="new">new</a>
</div>
